
rpc/ensure? for batch

config in a file
resolve fn symbols
check config (resolve fns, get specs)

json body -- parameter
split doc on sections?

tests for handler

;; review sequential?

;; make name

;; check config with spec

;; check functions in config
;; auto-load func ns in config

;; better explain (expound?)

;; method overrides

;; spec for config
;; validate config in handler

;; handler overrides
;; discover handler
;; rename functions

;; no-log field maybe?

;; rtfm

;; rename to doc
;; add to examples
;; move resources
;; dump to file function
